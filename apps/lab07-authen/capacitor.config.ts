import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cpkku.jakkaphat.lab07', // เปลี่ยนตรงนี้ให้เป็นชื่อของคุณ 
  appName: 'lab07-authen',
  webDir: 'dist'
};

export default config;